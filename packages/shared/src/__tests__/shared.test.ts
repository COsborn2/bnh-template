import { describe, test } from "bun:test";

describe("@app/shared", () => {
  test("placeholder", () => {
    console.log("No shared utilities to test yet — add tests here as the package grows.");
  });
});
