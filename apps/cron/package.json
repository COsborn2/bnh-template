{
  "name": "@app/cron",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "bun build src/cleanup.ts --outdir dist --target bun --production",
    "cleanup": "bun src/cleanup.ts",
    "start": "bun dist/cleanup.js",
    "lint": "tsc --noEmit"
  },
  "dependencies": {
    "@app/db": "workspace:*"
  },
  "devDependencies": {
    "@types/bun": "^1.3.12",
    "typescript": "^6.0.3"
  }
}
