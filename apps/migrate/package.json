{
  "name": "@app/migrate",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "bun build src/migrate.ts --outdir dist --target bun --production --sourcemap=linked",
    "migrate": "bun --env-file=../../.env src/migrate.ts",
    "start": "bun dist/migrate.js",
    "db:generate": "bun --env-file=../../.env node_modules/.bin/drizzle-kit generate",
    "db:migrate": "bun --env-file=../../.env src/migrate.ts",
    "lint": "tsc --noEmit",
    "test": "bun test"
  },
  "dependencies": {
    "@app/db": "workspace:*",
    "drizzle-orm": "^0.45.2",
    "postgres": "^3.4.9"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14",
    "drizzle-kit": "^0.31.10",
    "typescript": "^7.0.2"
  }
}
