/** Rows per page on the paged list screens (admin tables). Directive-free so
 *  server pages can build the same `limit`/`offset` query the client
 *  components page with. */
export const PAGE_SIZE = 20;
